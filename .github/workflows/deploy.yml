name: Rajasthan Exam CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: List project files
        run: ls -la

      - name: CI success message
        run: echo "CI pipeline ran successfully 🚀"
